![image-20260127020012126](C:\Users\16532\AppData\Roaming\Typora\typora-user-images\image-20260127020012126.png)

如果这里权限是drwxrws---会有问题，lm(nas组)用户会没有权限访问他

## 那 `s` 到底是什么？

`s` = **setgid（Set Group ID）**

在**目录**上时，它的行为是：

> **凡是在这个目录里新建的文件 / 子目录，组（group）都会自动继承为这个目录的组**

👉 组 `nas` **没有 x**，所以 `lm:nas` 的容器进不去
 于是 File Browser 报：`open /srv: permission denied`

所以应该按如下方式改：

```
sudo chmod 2775 /data/vault/knowledge
```

解释一下

- `2` → setgid（保留）
- `7` → owner rwx
- `7` → group rwx
- `5` → other r-x ✅