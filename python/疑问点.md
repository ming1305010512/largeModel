[TOC]



# 一、if __name__ == "__main__":的作用

`if __name__ == "__main__":`
 这下面的代码 **只在“这个文件被直接运行”时执行**，
 如果是“被别的文件 import 进来”，这下面的代码就不会跑。

## 一、`__name__` 是什么？

在每个 `.py` 文件里，Python 会自动帮你定义一个变量：`__name__`

- 当你 **直接运行这个文件** 时，比如：

  ```
  python myscript.py
  ```

  这时在 `myscript.py` 里：

  ```
  print(__name__)   # 输出 "__main__"
  ```

- 当你 **把这个文件当成模块 import** 时，比如有另外一个文件：

  ```
  import myscript
  ```

  这时在 `myscript.py` 里：

  ```
  print(__name__)   # 输出 "myscript"
  ```

**总结：**

- 直接运行：`__name__ == "__main__"`
- 被别的模块 import：`__name__ == "模块名"`

# 二、zip详解

## 一、`zip()` 是什么？

> `zip()` 用于将**多个可迭代对象中的元素**，按**位置一一配对**，返回一个 **zip 对象（可迭代）**。

换句话说，它是用来把多个列表、元组等“拉链式”地组合在一起的。

## 二、基本语法

```
zip(iterable1, iterable2, ..., iterableN)
```

- 返回一个可迭代的 zip 对象（不是列表）
- 可以用 `list()`、`tuple()`、`dict()` 等转换

## 三、基本示例

```
names = ['Alice', 'Bob', 'Charlie']
scores = [85, 90, 88]

zipped = zip(names, scores)
print(list(zipped))
```

### 输出：

```
[('Alice', 85), ('Bob', 90), ('Charlie', 88)]
```

## 四、长度不同时的处理方式

`zip()` 会**自动按最短序列截断**！

```
a = [1, 2, 3]
b = ['a', 'b']
print(list(zip(a, b)))
```

输出：

```
[(1, 'a'), (2, 'b')]  # 第3个元素被忽略
```

✅ 如果你想**补齐到最长**，可以使用 `itertools.zip_longest`。

##  五、应用

### 1. 并行遍历多个列表

```
for name, score in zip(names, scores):
    print(f"{name}: {score}")
```

------

### 2. 同时解包多个变量

```
pairs = [('A', 1), ('B', 2), ('C', 3)]
letters, numbers = zip(*pairs)
print(letters)  # ('A', 'B', 'C')
print(numbers)  # (1, 2, 3)
```

👉 这里用的是 **“解包 + zip反向”技巧**：`zip(*)` 可视为“列转行”

------

### 3. 构造字典

```
keys = ['name', 'age', 'city']
values = ['Tom', 25, 'Beijing']
my_dict = dict(zip(keys, values))
print(my_dict)
```

输出：

```
{'name': 'Tom', 'age': 25, 'city': 'Beijing'}
```

------

### 4. 交换二维列表的行列（转置）

```
matrix = [
    [1, 2, 3],
    [4, 5, 6]
]

transposed = list(zip(*matrix))
print(transposed)  # [(1, 4), (2, 5), (3, 6)]
```

## 六、zip对象特性（懒加载）

- `zip()` 返回的是一个 **迭代器对象**（zip object），不是立即生成结果的列表；
- 只能遍历一次，想要复用必须转换为 list/tuple 等。

```
z = zip([1, 2], ['a', 'b'])
print(z)         # <zip object at 0x...>
print(list(z))   # [('1', 'a'), ('2', 'b')]
print(list(z))   # [] 再次遍历为空了
```